# Python 期末复习闯关题库

> 本文件由 `app.py` 的题库**自动生成**，仅供查阅。改题请改 `app.py`。

> 标 **〔组员5 制作〕** 的为组员5新增题（共 11 道）。

题型：概念选择题 + 代码填空 + 代码阅读。取材自本课程实验/作业及《期末复习重点》(谭德健)。全库共 110 题。

## 简单模式

### 1. 变量与赋值

题目：不使用临时变量，交换两个变量 a、b 的值，正确写法是？

- A. a = b; b = a
- B. a, b = b, a
- C. swap(a, b)
- D. a == b

答案：B

解析：Python 用 a, b = b, a 一行同时赋值：右边先打包成元组再解包，无需临时变量。

### 2. 运算符

题目：表达式 17 // 5 的结果是？

- A. 3
- B. 3.4
- C. 2
- D. 3.0

答案：A

解析：// 是整除，只保留商的整数部分；17 / 5 才是 3.4。

### 3. 运算符

题目：表达式 17 % 5 的结果是？

- A. 2
- B. 3
- C. 3.4
- D. 0

答案：A

解析：% 取余数：17 = 5×3 + 2，余数是 2。判断奇偶、辗转相除都靠它。

### 4. 字符串

题目：s = 'Python'，s[1] 的结果是？

- A. 'P'
- B. 'y'
- C. 't'
- D. 'n'

答案：B

解析：下标从 0 开始：s[0] 是 'P'，s[1] 是 'y'。

### 5. 字符串

题目：s = 'abc'，切片 s[::-1] 的结果是？

- A. 'abc'
- B. 'cba'
- C. 'acb'
- D. 报错

答案：B

解析：切片 [::-1] 步长为 -1，表示整个字符串倒序，是反转字符串的常用写法。

### 6. 数据类型

题目：type('123') 表示的数据类型是？

- A. int
- B. float
- C. str
- D. bool

答案：C

解析：被引号包住就是字符串 str，哪怕里面写的是数字。

### 7. 列表

题目：lst = [5, 6, 7]，len(lst) 的结果是？

- A. 2
- B. 3
- C. 5
- D. 18

答案：B

解析：len() 返回元素个数，列表有 3 个元素。

### 8. 列表

题目：lst = [10, 20, 30]，lst[-1] 的结果是？

- A. 10
- B. 20
- C. 30
- D. 报错

答案：C

解析：负下标从末尾数起，-1 是最后一个元素 30。

### 9. 函数

题目：Python 中定义函数使用哪个关键字？

- A. def
- B. func
- C. function
- D. define

答案：A

解析：用 def 定义函数，return 用来返回结果。

### 10. 输出

题目：n = 5，print(f'{n * n}') 会输出什么？

- A. n * n
- B. 25
- C. 55
- D. 10

答案：B

解析：f-string 里 {} 中的表达式会被计算，n * n = 25。

### 11. 变量命名

题目：以下哪个是合法的变量名？

- A. 1num
- B. num-1
- C. num_1
- D. num@1

答案：C

解析：变量名只能由字母、数字、下划线组成，不能以数字开头，也不能含特殊符号。

### 12. 运算符

题目：print(2 ** 3) 的输出是？

- A. 5
- B. 6
- C. 8
- D. 9

答案：C

解析：** 是乘方（幂运算），2 的 3 次方 = 8。

### 13. 注释

题目：Python 单行注释使用哪个符号？

- A. //
- B. #
- C. /* */
- D. <!-- -->

答案：B

解析：Python 用 # 作单行注释；// 是其他语言的写法。

### 14. 列表

题目：list1 = [1, 2, 3]，list1[0] 的值是？

- A. 1
- B. 2
- C. 3
- D. 报错

答案：A

解析：列表下标从 0 开始，list1[0] 是第一个元素 1。

### 15. 流程控制

题目：下列哪个不是条件判断关键字？

- A. if
- B. elif
- C. else
- D. then

答案：D

解析：Python 用 if / elif / else，没有 then。

### 16. 流程控制

题目：在循环中，跳出整个循环用哪个关键字？

- A. continue
- B. break
- C. pass
- D. return

答案：B

解析：break 跳出整个循环；continue 只跳过本次、进入下一次。

### 17. 类型转换

题目：把字符串 '123' 转成整数，用哪个函数？

- A. str()
- B. int()
- C. float()
- D. bool()

答案：B

解析：int() 把内容转成整数；str() 转字符串、float() 转小数。

### 18. 字典

题目：dict1 = {'name': 'Tom', 'age': 18}，取出年龄的写法是？

- A. dict1[0]
- B. dict1['age']
- C. dict1.age
- D. dict1(age)

答案：B

解析：字典通过键取值，用中括号写键名：dict1['age']。

### 19. 数据类型

题目：布尔值 True 对应的数字是？

- A. 0
- B. 1
- C. -1
- D. 任意非 0 数

答案：B

解析：True 等价于 1，False 等价于 0，可以参与运算。

### 20. 运算符

题目：a = 10; b = 3，a % b 的结果是？

- A. 1
- B. 3
- C. 0
- D. 3.33

答案：A

解析：% 取余数：10 除以 3 商 3 余 1。

### 21. 字符串

题目：字符串拼接使用哪个符号？

- A. +
- B. -
- C. *
- D. /

答案：A

解析：+ 把两个字符串连接起来，如 'ab' + 'cd' = 'abcd'。

### 22. 列表

题目：定义一个空列表，正确的写法是？

- A. lst = []
- B. lst = {}
- C. lst = ()
- D. lst = 0

答案：A

解析：[] 是空列表；{} 是空字典，() 是空元组。

### 23. 循环

题目：for 循环常搭配哪个函数生成连续数字序列？

- A. range
- B. len
- C. input
- D. type

答案：A

解析：range 生成连续整数序列，如 range(5) 是 0,1,2,3,4。

### 24. Python基础

题目：Python 源程序文件的后缀名是？

- A. .py
- B. .python
- C. .pt
- D. .pic

答案：A

解析：Python 源程序文件以 .py 为后缀。

### 25. 类型转换

题目：list('abc') 的结果是？

- A. ['a', 'b', 'c']
- B. 'abc'
- C. ['abc']
- D. 报错

答案：A

解析：list() 把可迭代对象拆成元素列表，字符串会拆成单个字符。

## 普通模式

### 26. 流程控制

题目：range(2, 8, 2) 依次产生哪些整数？

- A. 2, 4, 6
- B. 2, 4, 6, 8
- C. 2, 3, 4, 5, 6, 7
- D. 2, 8

答案：A

解析：range(start, stop, step) 含 start 不含 stop，步长 2：2, 4, 6（8 取不到）。

### 27. 输入处理

题目：'3,5,7'.split(',') 的结果是？

- A. [3, 5, 7]
- B. ['3', '5', '7']
- C. '3 5 7'
- D. (3, 5, 7)

答案：B

解析：split 切成字符串列表，元素仍是字符串！要参与计算得先用 int() 或 float() 转换——输入处理最常见的坑。

### 28. 列表推导式

题目：[i * i for i in range(1, 4)] 的结果是？

- A. [1, 4, 9]
- B. [1, 2, 3]
- C. [1, 4, 9, 16]
- D. [0, 1, 4]

答案：A

解析：列表生成式对 1, 2, 3 各求平方得 1, 4, 9；range(1, 4) 不含 4。

### 29. 列表

题目：lst = [1, 2, 3]; lst.append([4, 5]) 之后，lst 是？

- A. [1, 2, 3, 4, 5]
- B. [1, 2, 3, [4, 5]]
- C. [1, 2, 3, 4]
- D. 报错

答案：B

解析：append 把参数当成一个元素整体加进去，所以末尾是一个子列表；要展开成 4、5 得用 extend 或 +。

### 30. 字典

题目：d = {'a': 1, 'b': 2}，下面哪个会触发 KeyError？

- A. d['a']
- B. d['c']
- C. d.get('c')
- D. len(d)

答案：B

解析：键 'c' 不存在，用中括号取值会 KeyError；而 d.get('c') 返回 None，不报错。

### 31. 循环

题目：for i, v in enumerate(['a', 'b', 'c']): 第一次循环时 i 和 v 分别是？

- A. 1 和 'a'
- B. 0 和 'a'
- C. 0 和 'b'
- D. 'a' 和 0

答案：B

解析：enumerate 默认从 0 开始配下标，第一次 i = 0、v = 'a'。

### 32. 函数式

题目：list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])) 的结果是？

- A. [1, 3]
- B. [2, 4]
- C. [1, 2, 3, 4]
- D. [True, False, True, False]

答案：B

解析：filter 只保留让 lambda 返回 True 的元素，这里是偶数 2 和 4。

### 33. 集合

题目：set([1, 2, 2, 3, 3, 3]) 的结果是？

- A. [1, 2, 3]
- B. {1, 2, 3}
- C. {1, 2, 2, 3, 3, 3}
- D. (1, 2, 3)

答案：B

解析：集合 set 自动去重，用花括号表示；常用来判断元素是否已经出现过。

### 34. 统计

题目：lst = [3, 9, 2, 8]，sum(1 for v in lst if v > 5) 的结果是？

- A. 2
- B. 17
- C. 4
- D. 5

答案：A

解析：对每个大于 5 的元素计 1 再求和，相当于统计个数：9 和 8 共 2 个。

### 35. 文件与异常

题目：用 open() 打开一个不存在的文件，应该捕获哪个异常？

- A. ValueError
- B. FileNotFoundError
- C. KeyError
- D. TypeError

答案：B

解析：文件不存在时抛 FileNotFoundError，用 try ... except FileNotFoundError 捕获并友好提示。

### 36. 代码填空·输入处理

题目：键盘输入用逗号分隔的数字，求它们的和。____ 处应填什么？

```python
s = input("输入数字，用逗号分隔：")   # 例如 "3,5,7"
nums = [____ for v in s.split(",")]
print(sum(nums))                      # 期望输出 15
```

- A. float(v)
- B. v
- C. str(v)
- D. v.split()

答案：A

解析：split 得到的是字符串列表，必须先转成数字（float 或 int）才能求和；直接对字符串求和会报错。

### 37. 代码填空·文件异常

题目：打开文件读取内容，文件不存在时给出提示。except 后的 ____ 应填什么？

```python
try:
    with open("data.txt", "r", encoding="utf-8") as f:
        print(f.read())
except ____:
    print("文件不存在！")
```

- A. FileNotFoundError
- B. ValueError
- C. KeyError
- D. IndexError

答案：A

解析：文件不存在时抛出的是 FileNotFoundError，捕获它即可给出友好提示。

### 38. 代码填空·循环累加

题目：用循环累加 1 到 100 的和。range 的 ____ 应填什么？

```python
s = 0
for i in range(____):
    s += i
print(s)        # 期望输出 5050
```

- A. 1, 101
- B. 1, 100
- C. 0, 100
- D. 1, 99

答案：A

解析：range 右端点取不到，要累加到 100 必须写 range(1, 101)。

### 39. 代码填空·去重保序

题目：去除列表重复元素并保留原始顺序。if 后的 ____ 应填什么？

```python
def remove_duplicates(lst):
    seen = set()
    result = []
    for item in lst:
        if ____:
            seen.add(item)
            result.append(item)
    return result
```

- A. item not in seen
- B. item in seen
- C. item not in result
- D. item == seen

答案：A

解析：只有没见过的元素才加入结果；用 set 的 not in 判断高效，既去重又保留顺序。

### 40. 随机模块

题目：random.random() 生成的随机数范围是？

- A. [0, 1)
- B. [0, 1]
- C. (0, 1)
- D. [1, 10]

答案：A

解析：random.random() 返回 0（含）到 1（不含）之间的浮点数。

### 41. 集合

题目：创建一个空集合，正确的是？

- A. s = set()
- B. s = {}
- C. s = []
- D. s = ()

答案：A

解析：{} 创建的是空字典；空集合只能用 set()。

### 42. 文件操作

题目：open 的模式 'a' 表示？

- A. 追加写入（append）
- B. 只读
- C. 二进制
- D. 新建并覆盖

答案：A

解析：a = append 追加，从文件末尾写；r=read, w=write, b=binary。

### 43. 文件操作

题目：open 的模式 'w' 的特点是？

- A. 覆盖写入（文件已存在则先清空）
- B. 只读不能写
- C. 追加到末尾
- D. 文件不存在会报错

答案：A

解析：w 模式打开会清空原内容再写；要保留旧内容追加得用 a。

### 44. 可变性

题目：下列哪个是不可变（immutable）数据类型？

- A. 元组
- B. 列表
- C. 字典
- D. 集合

答案：A

解析：不可变：数值、字符串、元组；可变：列表、字典、集合。

### 45. 字典

题目：字典的键（key）必须满足什么？

- A. 不可变且唯一
- B. 可以重复
- C. 必须是字符串
- D. 必须是数字

答案：A

解析：键必须是不可变类型（字符串、数字、元组）且唯一，值可以是任意类型。

### 46. 列表方法

题目：lst.pop() 不带参数时的行为是？

- A. 移除并返回最后一个元素
- B. 移除第一个元素
- C. 清空列表
- D. 返回列表长度

答案：A

解析：pop() 默认移除并返回最后一个元素；pop(i) 移除指定位置。

### 47. 列表方法

题目：lst.insert(0, 5) 的作用是？

- A. 在索引 0 处插入 5
- B. 在末尾添加 5
- C. 删除索引 0 的元素
- D. 把第 0 个改成 5

答案：A

解析：insert(位置, 元素) 在指定索引处插入，后面的元素依次后移。

### 48. 字符串方法

题目：'-'.join(['a', 'b', 'c']) 的结果是？

- A. 'a-b-c'
- B. ['a', 'b', 'c']
- C. 'abc'
- D. 'a,b,c'

答案：A

解析：join 用分隔符把可迭代对象的元素连成一个字符串。

### 49. 字符串方法

题目：'  hi  '.strip() 的结果是？

- A. 'hi'
- B. '  hi'
- C. 'hi  '
- D. '  hi  '

答案：A

解析：strip() 去掉字符串首尾的空白字符。

### 50. 字符串方法

题目：'123'.isdigit() 的结果是？

- A. True
- B. False
- C. 123
- D. 报错

答案：A

解析：isdigit() 判断字符串是否全是数字，全数字返回 True。

### 51. 字符串格式化

题目：'{} 和 {}'.format('A', 'B') 的结果是？

- A. 'A 和 B'
- B. '{} 和 {}'
- C. 'AB'
- D. 报错

答案：A

解析：format 按顺序把 {} 替换成参数。

### 52. 字符串运算符

题目：'ab' * 3 的结果是？

- A. 'ababab'
- B. 'ab3'
- C. 'aaabbb'
- D. 报错

答案：A

解析：字符串 * 整数 表示复制重复，'ab' 重复 3 次。

### 53. 字符串运算符

题目：'H' in 'Hello' 的结果是？

- A. True
- B. False
- C. 0
- D. 'H'

答案：A

解析：in 判断子串是否存在，'H' 在 'Hello' 中，返回 True。

### 54. 列表与元组

题目：列表和元组最本质的区别是？

- A. 列表可变、元组不可变
- B. 列表用圆括号、元组用方括号
- C. 元组能存数字、列表不能
- D. 没有区别

答案：A

解析：本质区别是可变性：列表 [] 可增删改，元组 () 一旦创建不能改。

### 55. 切片

题目：lst = [0, 1, 2, 3, 4]，lst[1:4] 的结果是？

- A. [1, 2, 3]
- B. [1, 2, 3, 4]
- C. [0, 1, 2, 3]
- D. [2, 3, 4]

答案：A

解析：切片 [1:4] 含起点 1 不含终点 4，取索引 1、2、3。

### 56. 切片

题目：lst = [0, 1, 2, 3, 4, 5]，lst[::2] 的结果是？

- A. [0, 2, 4]
- B. [1, 3, 5]
- C. [0, 1, 2]
- D. [5, 3, 1]

答案：A

解析：[::2] 步长为 2，从头每隔一个取，得到索引 0、2、4。

### 57. 列表方法

题目：lst = [1, 2]; lst.extend([3, 4]) 后，lst 是？

- A. [1, 2, 3, 4]
- B. [1, 2, [3, 4]]
- C. [3, 4, 1, 2]
- D. [1, 2]

答案：A

解析：extend 把另一个列表的元素逐个加到末尾；append([3,4]) 才会变嵌套。

### 58. 字符串方法

题目：'aXbXc'.replace('X', '-') 的结果是？

- A. 'a-b-c'
- B. 'aXbXc'
- C. 'abc'
- D. 'a-bXc'

答案：A

解析：replace 把所有匹配的子串都替换掉。

### 59. 字符串方法

题目：'abcabc'.count('a') 的结果是？

- A. 2
- B. 1
- C. 3
- D. 0

答案：A

解析：count 统计子串出现次数，'a' 出现 2 次。

### 60. 字符串方法

题目：'hello'.find('l') 的结果是？

- A. 2
- B. 3
- C. -1
- D. 1

答案：A

解析：find 返回子串第一次出现的下标，第一个 'l' 在索引 2；找不到返回 -1。

### 61. 字符串方法

题目：'hello world'.title() 的结果是？

- A. 'Hello World'
- B. 'HELLO WORLD'
- C. 'Hello world'
- D. 'hello world'

答案：A

解析：title() 把每个单词的首字母都变大写。

### 62. 字符串方法

题目：'python'.startswith('py') 的结果是？

- A. True
- B. False
- C. 'py'
- D. 报错

答案：A

解析：startswith 判断字符串是否以指定前缀开头，返回布尔值。

### 63. 循环else

题目：for 循环正常结束（没有被 break 中断）时，会执行 else 子句吗？

- A. 会执行
- B. 不会执行
- C. 报错
- D. 随机决定

答案：A

解析：循环正常跑完会执行 else；若中途 break 退出，则不执行 else。

### 64. 模块导入

题目：import math 之后，调用平方根函数要写成？

- A. math.sqrt(x)
- B. sqrt(x)
- C. math(sqrt(x))
- D. import sqrt

答案：A

解析：import math 要用 模块名.函数名；想直接写 sqrt() 得用 from math import sqrt。

### 65. 类型转换

题目：tuple([1, 2, 3]) 的结果是？

- A. (1, 2, 3)
- B. [1, 2, 3]
- C. {1, 2, 3}
- D. 报错

答案：A

解析：tuple() 把可迭代对象转成元组。

## 挑战模式

### 66. 排序

题目：选择排序每一轮的核心操作是？

- A. 相邻两个元素比较并不断交换
- B. 在未排序部分找到最小值，放到当前位置
- C. 随机交换两个元素
- D. 把整个列表反转一次

答案：B

解析：选择排序每轮在内层找到最小值的下标，内层结束后只交换一次；相邻比较、内层反复交换的是冒泡排序。

### 67. 排序

题目：选择排序中，交换元素的语句应该写在哪里？

- A. 写在内层 if 里面
- B. 写在内层 for 循环外面（外层 for 之内）
- C. 写在所有循环外面
- D. 选择排序不需要交换

答案：B

解析：内层只更新最小值下标 min_idx，内层结束后才交换一次。写进 if 里就变成冒泡了——这是最常考的区别。

### 68. 二维列表

题目：执行 m = [[0]*3]*2; m[0][0] = 1 之后，m 是？

- A. [[1, 0, 0], [0, 0, 0]]
- B. [[1, 0, 0], [1, 0, 0]]
- C. [[1, 1, 1], [0, 0, 0]]
- D. 报错

答案：B

解析：[[0]*3]*2 让两行指向同一个列表，改一行另一行也跟着变！正确写法是 [[0]*3 for _ in range(2)]。

### 69. 二维列表

题目：一个 2 行 4 列的矩阵，转置后变成几行几列？

- A. 2 行 4 列
- B. 4 行 2 列
- C. 4 行 4 列
- D. 2 行 2 列

答案：B

解析：转置把行列互换：原 m[i][j] 变成新 result[j][i]，所以 2×4 变 4×2。

### 70. 二维列表

题目：n×n 矩阵 matrix 的主对角线元素下标是？

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[0][i]
- D. matrix[i][0]

答案：A

解析：主对角线行号等于列号，即 matrix[i][i]；副对角线才是 matrix[i][n-1-i]。

### 71. 二维列表

题目：n×n 矩阵的副对角线（右上到左下）元素下标是？

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[n-i][i]
- D. matrix[i][n+i]

答案：B

解析：副对角线满足 行号 + 列号 = n-1，所以列下标是 n-1-i。

### 72. 数论算法

题目：辗转相除法 while b: a, b = b, a % b; return a，求 gcd(12, 18) 的结果是？

- A. 2
- B. 3
- C. 6
- D. 36

答案：C

解析：(12,18)→(18,12)→(12,6)→(6,0)，b 为 0 时返回 a = 6。12 与 18 的最大公约数是 6。

### 73. 数论算法

题目：已知 gcd(a, b)，求最小公倍数 lcm 的正确写法是？

- A. a * b / gcd(a, b)
- B. a * b // gcd(a, b)
- C. a + b - gcd(a, b)
- D. gcd(a, b) // (a * b)

答案：B

解析：lcm = a*b/gcd，要用整除 // 得到整数；用 / 会得到带小数的 float。

### 74. 参数传递

题目：def f(lst): lst.append(0)。执行 a = [1, 2]; f(a) 之后，a 是？

- A. [1, 2]
- B. [1, 2, 0]
- C. [0, 1, 2]
- D. None

答案：B

解析：列表是可变对象，函数内对它 append 会直接改到原列表；而整数等不可变对象不会被函数改变。

### 75. 参数传递

题目：def f(x): x += 1; return x。执行 a = 5; b = f(a) 之后，a 和 b 分别是？

- A. 5 和 6
- B. 6 和 6
- C. 5 和 5
- D. 6 和 5

答案：A

解析：整数不可变，函数内 x += 1 只改局部副本并返回 6，原 a 仍是 5。可与上一题的列表对照记忆。

### 76. 递归

题目：递归判断素数 def is_prime(num, i=2): 中，返回 True 的终止条件通常是？

- A. num == 0
- B. i * i > num
- C. i > num
- D. num % i == 0

答案：B

解析：当 i 的平方已超过 num，说明没找到能整除的因子，num 是素数，返回 True；只需查到 √num 即可。

### 77. 去重算法

题目：要给列表去重并保留原始顺序，用哪种结构记录“已出现过”最高效？

- A. 再开一个 list，每次用 in 查找
- B. 用 set 记录已出现的元素
- C. 把所有元素拼成字符串
- D. 用 sort 排序后再比较

答案：B

解析：set 的成员判断 in 接近 O(1)，比在 list 里查找快；遍历时没见过就加入结果并记进 set，既去重又保序。

### 78. 代码填空·选择排序

题目：选择排序（升序）。内层找最小值的判断 ____ 应填什么？

```python
def selection_sort(arr):
    n = len(arr)
    for i in range(n - 1):
        min_idx = i
        for j in range(i + 1, n):
            if ____:          # 找更小的
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr
```

- A. arr[j] < arr[min_idx]
- B. arr[j] > arr[min_idx]
- C. arr[j] < arr[i]
- D. j < min_idx

答案：A

解析：要在未排序区找最小值，需比较 arr[j] 与当前最小 arr[min_idx]；写成 > 会变降序，比较 arr[i] 是常见错误。

### 79. 代码填空·辗转相除

题目：辗转相除法求最大公约数。循环体内 ____ 应填什么？

```python
def gcd(a, b):
    while b != 0:
        a, b = ____
    return a
```

- A. b, a % b
- B. a % b, b
- C. b, a // b
- D. a, b % a

答案：A

解析：新的 a 取旧 b，新的 b 取 a 除以 b 的余数，直到 b 为 0，此时 a 即最大公约数。

### 80. 代码填空·矩阵转置

题目：矩阵转置，把原矩阵行列互换。____ 处应填什么？

```python
def transpose(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            ____ = matrix[i][j]
    return result
```

- A. result[j][i]
- B. result[i][j]
- C. result[i][i]
- D. matrix[j][i]

答案：A

解析：转置就是行列下标互换：原 matrix[i][j] 放到新 result[j][i]。

### 81. 代码填空·二维列表初始化

题目：创建 3 行 4 列、初值全 0 且各行互相独立的二维列表。____ 应填什么？

```python
rows, cols = 3, 4
matrix = ____
matrix[0][0] = 1
print(matrix)   # 期望只改到第 0 行
```

- A. [[0]*cols for _ in range(rows)]
- B. [[0]*cols]*rows
- C. [0]*cols*rows
- D. [[0]*rows]*cols

答案：A

解析：[[0]*cols]*rows 会让各行共享同一引用，改一行全变；必须用列表推导式生成相互独立的行。

### 82. 代码填空·递归素数

题目：递归判断 num 是否为素数。返回 True 的终止条件 ____ 应填什么？

```python
def is_prime(num, i=2):
    if num < 2:
        return False
    if ____:
        return True
    if num % i == 0:
        return False
    return is_prime(num, i + 1)
```

- A. i * i > num
- B. i > num
- C. num % i == 0
- D. i == num

答案：A

解析：试除到 √num 即可；当 i*i > num 仍没找到因子，说明 num 是素数，返回 True。

### 83. 代码填空·主对角线

题目：求 n×n 矩阵主对角线元素之和。____ 应填什么？

```python
def diag_sum(matrix):
    n = len(matrix)
    s = 0
    for i in range(n):
        s += ____
    return s
```

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[0][i]
- D. matrix[i][0]

答案：A

解析：主对角线行号等于列号，累加 matrix[i][i]；若要副对角线则是 matrix[i][n-1-i]。

### 84. 代码填空·lambda 过滤

题目：用 lambda + filter 删除列表里所有的 'A'。____ 应填什么？

```python
lst = list("ABCAAD")
result = list(filter(____, lst))
print("".join(result))   # 期望输出 BCD
```

- A. lambda x: x != 'A'
- B. lambda x: x == 'A'
- C. lambda x: 'A'
- D. x != 'A'

答案：A

解析：filter 保留让函数返回 True 的元素；要删掉 'A'，就保留 x != 'A' 的元素。

### 85. 排序　〔组员5 制作〕

题目：冒泡排序按升序排列时，第一轮完整比较结束后，哪个元素通常会被放到列表末尾？

- A. 当前最大值
- B. 当前最小值
- C. 第一个元素
- D. 随机元素

答案：A

解析：升序冒泡排序每轮不断交换相邻逆序元素，第一轮会把当前最大值一路“冒泡”到末尾。

### 86. 递归　〔组员5 制作〕

题目：递归函数最容易出错的地方是什么？

- A. 没有终止条件或没有向终止条件靠近
- B. 函数名太短
- C. 用了 return
- D. 参数数量超过 1 个

答案：A

解析：递归必须有明确终止条件，并且每次递归调用都要让问题规模变小，否则可能无限递归直到 RecursionError。

### 87. 二维列表　〔组员5 制作〕

题目：复制二维列表时，matrix[:] 的特点是？

- A. 只复制外层列表，内层列表仍共享
- B. 能完整深拷贝所有层
- C. 会把二维列表转成一维列表
- D. 一定会报错

答案：A

解析：matrix[:] 是浅拷贝，只新建外层列表；里面每一行仍然指向原来的列表，修改某一行内容会互相影响。

### 88. 字典排序　〔组员5 制作〕

题目：sorted(d.items(), key=lambda item: item[1]) 的排序依据是？

- A. 按字典的值排序
- B. 按字典的键排序
- C. 按键值对数量排序
- D. 按插入顺序反转

答案：A

解析：d.items() 产生 (键, 值) 形式的元组，item[1] 取的是值，所以会按字典的值排序。

### 89. 文件与异常　〔组员5 制作〕

题目：try...except...else...finally 中，finally 代码块什么时候执行？

- A. 无论是否发生异常都会执行
- B. 只有发生异常才执行
- C. 只有没有异常才执行
- D. 只有 return 后才执行

答案：A

解析：finally 常用于关闭文件、释放资源等收尾操作，不管 try 中是否出错，通常都会执行。

### 90. 代码阅读·默认参数　〔组员5 制作〕

题目：阅读代码，程序两次 print 的输出分别是什么？

```python
def collect(x, box=[]):
    box.append(x)
    return box

print(collect(1))
print(collect(2))
```

- A. [1] 和 [2]
- B. [1] 和 [1, 2]
- C. [1, 2] 和 [1, 2]
- D. 报错

答案：B

解析：默认参数中的列表只会在函数定义时创建一次，两次调用共用同一个 box，所以第二次输出 [1, 2]。

### 91. 代码阅读·浅拷贝　〔组员5 制作〕

题目：阅读代码，print(a) 的输出是什么？

```python
a = [[1], [2]]
b = a[:]
b[0].append(9)
print(a)
```

- A. [[1], [2]]
- B. [[1, 9], [2]]
- C. [[1], [2, 9]]
- D. [[9], [2]]

答案：B

解析：a[:] 只复制外层列表，b[0] 和 a[0] 指向同一个内层列表；append(9) 会影响 a[0]。

### 92. 代码阅读·异常流程　〔组员5 制作〕

题目：阅读代码，输出顺序是什么？

```python
try:
    x = int("8")
except ValueError:
    print("E")
else:
    print("OK")
finally:
    print("END")
```

- A. E → END
- B. OK → END
- C. 只输出 OK
- D. 只输出 END

答案：B

解析：int('8') 不会触发 ValueError，所以执行 else 输出 OK；finally 无论如何都会执行，继续输出 END。

### 93. 代码阅读·字典排序　〔组员5 制作〕

题目：阅读代码，print(result[0]) 的输出是什么？

```python
scores = {"Li": 88, "Wang": 95, "Zhao": 90}
result = sorted(scores.items(), key=lambda item: item[1], reverse=True)
print(result[0])
```

- A. ('Li', 88)
- B. ('Wang', 95)
- C. ('Zhao', 90)
- D. ('Wang', 88)

答案：B

解析：lambda item: item[1] 表示按分数排序，reverse=True 表示降序，最高分是 Wang 的 95。

### 94. 代码阅读·二维列表　〔组员5 制作〕

题目：阅读代码，print(list(zip(*matrix))) 的输出是什么？

```python
matrix = [[1, 2, 3], [4, 5, 6]]
print(list(zip(*matrix)))
```

- A. [(1, 4), (2, 5), (3, 6)]
- B. [[1, 4], [2, 5], [3, 6]]
- C. [(1, 2, 3), (4, 5, 6)]
- D. [1, 4, 2, 5, 3, 6]

答案：A

解析：*matrix 会把两行拆开传给 zip，zip 按列配对，所以得到三组元组：(1,4)、(2,5)、(3,6)。

### 95. 代码阅读·递归　〔组员5 制作〕

题目：阅读代码，程序输出什么？

```python
def digit_sum(n):
    if n == 0:
        return 0
    return n % 10 + digit_sum(n // 10)

print(digit_sum(305))
```

- A. 8
- B. 35
- C. 305
- D. 递归错误

答案：A

解析：digit_sum 每次取个位并递归处理剩余数字：305 的各位数字和是 3 + 0 + 5 = 8。

### 96. 随机模块

题目：random.sample(pop, k) 的作用是？

- A. 从序列中随机取 k 个不重复元素组成列表
- B. 返回一个 0~1 的随机小数
- C. 原地打乱序列
- D. 返回最大的 k 个元素

答案：A

解析：sample 从序列里随机抽 k 个不重复元素，不改变原序列，常用于抽奖。

### 97. 集合运算

题目：求两个集合的并集，用哪个运算符？

- A. |
- B. &
- C. -
- D. ^

答案：A

解析：并集 |（union）、交集 &（intersection）、差集 -、对称差 ^。

### 98. 集合运算

题目：集合运算中，& 表示？

- A. 交集
- B. 并集
- C. 差集
- D. 对称差

答案：A

解析：& 是交集（两个集合都有的元素），对应方法 intersection()。

### 99. del与clear

题目：清空列表全部元素但保留列表对象本身，用？

- A. lst.clear()
- B. del lst
- C. lst.remove()
- D. lst.pop(all)

答案：A

解析：clear() 清空所有元素、容器还在；del lst 把整个变量都删掉。

### 100. 可变参数

题目：函数形参 *args 会把多个位置参数收集成？

- A. 元组
- B. 列表
- C. 字典
- D. 字符串

答案：A

解析：* 前缀收集任意个位置参数，存为元组；** 收集关键字参数存为字典。

### 101. 可变参数

题目：函数形参 **kwargs 会把关键字参数收集成？

- A. 字典
- B. 元组
- C. 列表
- D. 集合

答案：A

解析：** 前缀把 key=value 形式的关键字参数收集成字典。

### 102. 模块导入

题目：from math import sqrt 的作用是？

- A. 只引入 math 模块里的 sqrt 函数
- B. 引入整个 math 模块
- C. 定义一个 sqrt 函数
- D. 删除 sqrt

答案：A

解析：from 模块 import 名字 只引入指定成员，可直接写 sqrt()；import math 则要写 math.sqrt()。

### 103. 函数返回

题目：函数里写 return a, b，实际返回的是？

- A. 一个元组 (a, b)
- B. 两个独立的值
- C. 一个列表 [a, b]
- D. 报错

答案：A

解析：返回多个值时 Python 会打包成一个元组返回（本质仍是一个返回值）。

### 104. 排序

题目：sorted(lst) 和 lst.sort() 的区别是？

- A. sorted 返回新列表不改原；sort 原地排序无返回
- B. 完全一样
- C. sorted 原地改；sort 返回新列表
- D. 两个都会报错

答案：A

解析：sorted() 返回排好序的新列表、原列表不变；list.sort() 直接在原列表上排序，返回 None。

### 105. 代码阅读·lambda

题目：阅读代码，print 输出什么？

```python
students = [{"name": "A", "age": 20, "grade": 85},
            {"name": "B", "age": 18, "grade": 92}]
best = max(students, key=lambda s: s["grade"])
print(best["name"])
```

- A. B
- B. A
- C. 92
- D. 85

答案：A

解析：max 用 key=lambda 取每人的 grade 作比较，最高分 92 是 B，所以输出 B 的名字。

### 106. 代码阅读·列表推导式

题目：阅读代码，print(even) 输出什么？

```python
nums = [1, 2, 3]
squared = [x ** 2 for x in nums]
even = [x for x in squared if x % 2 == 0]
print(even)
```

- A. [4]
- B. [1, 4, 9]
- C. [2]
- D. [4, 9]

答案：A

解析：squared = [1, 4, 9]；再筛选偶数只剩 4，所以 even = [4]。

### 107. 代码阅读·列表方法

题目：阅读代码，print(nums) 输出什么？

```python
nums = [1, 2, 3]
nums.append(4)
nums.insert(0, 0)
nums.remove(2)
print(nums)
```

- A. [0, 1, 3, 4]
- B. [0, 1, 2, 3, 4]
- C. [1, 3, 4]
- D. [0, 1, 2, 4]

答案：A

解析：append(4)→[1,2,3,4]；insert(0,0)→[0,1,2,3,4]；remove(2) 删第一个 2→[0,1,3,4]。

### 108. 代码填空·递归

题目：补全 Fibonacci 递归函数，____ 处应填什么？

```python
def fib(n):
    if n <= 0:
        return 0
    if n == 1:
        return 1
    return ____

print(fib(6))
```

- A. fib(n-1) + fib(n-2)
- B. fib(n-1) * fib(n-2)
- C. n-1 + n-2
- D. fib(n) + fib(n-1)

答案：A

解析：Fibonacci 第 n 项 = 前两项之和，递归调用 fib(n-1) + fib(n-2)。

### 109. type与id

题目：id() 函数返回的是？

- A. 对象的唯一标识（内存地址）
- B. 对象的类型
- C. 对象的长度
- D. 对象的值

答案：A

解析：id() 返回对象唯一标识（通常是内存地址），可判断两个变量是否指向同一对象；type() 才是看类型。

### 110. 文件模式

题目：文件模式 'r+' 与 'w+' 的区别是？

- A. r+ 不清空原内容，w+ 会先清空
- B. 完全一样
- C. r+ 只能读
- D. w+ 只能读

答案：A

解析：两者都能读写；但 r+ 保留原内容，w+ 打开就清空原文件。
