# Python 期末复习闯关题库

> 本文件由 `gen_md.py` 从 `app.py` 的题库**自动生成**，仅供查阅。改题请改 `app.py`，再重跑 `python gen_md.py`。

> 标 **〔组员5 制作〕** 的为组员5新增题（共 11 道）。

题型：概念选择题 + 代码输出题 + 代码填空 + 代码阅读。取材自本课程实验/作业、《期末复习重点》(谭德健)，并整合了「普通模式题库」「Python入门60道」「扩充完整版」等题库 docx。全库共 195 题（简单模式 45 题 + 普通模式 78 题 + 挑战模式 72 题）。

## 简单模式

### 1. 变量与赋值

题目：不使用临时变量，交换两个变量 a、b 的值，正确写法是？

- A. a = b; b = a
- B. a, b = b, a
- C. swap(a, b)
- D. a == b

答案：B

解析：Python 用 a, b = b, a 一行同时赋值：右边先打包成元组再解包，无需临时变量。

### 2. 运算符

题目：表达式 17 // 5 的结果是？

- A. 3
- B. 3.4
- C. 2
- D. 3.0

答案：A

解析：// 是整除，只保留商的整数部分；17 / 5 才是 3.4。

### 3. 运算符

题目：表达式 17 % 5 的结果是？

- A. 2
- B. 3
- C. 3.4
- D. 0

答案：A

解析：% 取余数：17 = 5×3 + 2，余数是 2。判断奇偶、辗转相除都靠它。

### 4. 字符串

题目：s = 'Python'，s[1] 的结果是？

- A. 'P'
- B. 'y'
- C. 't'
- D. 'n'

答案：B

解析：下标从 0 开始：s[0] 是 'P'，s[1] 是 'y'。

### 5. 字符串

题目：s = 'abc'，切片 s[::-1] 的结果是？

- A. 'abc'
- B. 'cba'
- C. 'acb'
- D. 报错

答案：B

解析：切片 [::-1] 步长为 -1，表示整个字符串倒序，是反转字符串的常用写法。

### 6. 数据类型

题目：type('123') 表示的数据类型是？

- A. int
- B. float
- C. str
- D. bool

答案：C

解析：被引号包住就是字符串 str，哪怕里面写的是数字。

### 7. 列表

题目：lst = [5, 6, 7]，len(lst) 的结果是？

- A. 2
- B. 3
- C. 5
- D. 18

答案：B

解析：len() 返回元素个数，列表有 3 个元素。

### 8. 列表

题目：lst = [10, 20, 30]，lst[-1] 的结果是？

- A. 10
- B. 20
- C. 30
- D. 报错

答案：C

解析：负下标从末尾数起，-1 是最后一个元素 30。

### 9. 函数

题目：Python 中定义函数使用哪个关键字？

- A. def
- B. func
- C. function
- D. define

答案：A

解析：用 def 定义函数，return 用来返回结果。

### 10. 输出

题目：n = 5，print(f'{n * n}') 会输出什么？

- A. n * n
- B. 25
- C. 55
- D. 10

答案：B

解析：f-string 里 {} 中的表达式会被计算，n * n = 25。

### 11. 变量命名

题目：以下哪个是合法的变量名？

- A. 1num
- B. num-1
- C. num_1
- D. num@1

答案：C

解析：变量名只能由字母、数字、下划线组成，不能以数字开头，也不能含特殊符号。

### 12. 运算符

题目：print(2 ** 3) 的输出是？

- A. 5
- B. 6
- C. 8
- D. 9

答案：C

解析：** 是乘方（幂运算），2 的 3 次方 = 8。

### 13. 注释

题目：Python 单行注释使用哪个符号？

- A. //
- B. #
- C. /* */
- D. <!-- -->

答案：B

解析：Python 用 # 作单行注释；// 是其他语言的写法。

### 14. 列表

题目：list1 = [1, 2, 3]，list1[0] 的值是？

- A. 1
- B. 2
- C. 3
- D. 报错

答案：A

解析：列表下标从 0 开始，list1[0] 是第一个元素 1。

### 15. 流程控制

题目：下列哪个不是条件判断关键字？

- A. if
- B. elif
- C. else
- D. then

答案：D

解析：Python 用 if / elif / else，没有 then。

### 16. 流程控制

题目：在循环中，跳出整个循环用哪个关键字？

- A. continue
- B. break
- C. pass
- D. return

答案：B

解析：break 跳出整个循环；continue 只跳过本次、进入下一次。

### 17. 类型转换

题目：把字符串 '123' 转成整数，用哪个函数？

- A. str()
- B. int()
- C. float()
- D. bool()

答案：B

解析：int() 把内容转成整数；str() 转字符串、float() 转小数。

### 18. 字典

题目：dict1 = {'name': 'Tom', 'age': 18}，取出年龄的写法是？

- A. dict1[0]
- B. dict1['age']
- C. dict1.age
- D. dict1(age)

答案：B

解析：字典通过键取值，用中括号写键名：dict1['age']。

### 19. 数据类型

题目：布尔值 True 对应的数字是？

- A. 0
- B. 1
- C. -1
- D. 任意非 0 数

答案：B

解析：True 等价于 1，False 等价于 0，可以参与运算。

### 20. 运算符

题目：a = 10; b = 3，a % b 的结果是？

- A. 1
- B. 3
- C. 0
- D. 3.33

答案：A

解析：% 取余数：10 除以 3 商 3 余 1。

### 21. 字符串

题目：字符串拼接使用哪个符号？

- A. +
- B. -
- C. *
- D. /

答案：A

解析：+ 把两个字符串连接起来，如 'ab' + 'cd' = 'abcd'。

### 22. 列表

题目：定义一个空列表，正确的写法是？

- A. lst = []
- B. lst = {}
- C. lst = ()
- D. lst = 0

答案：A

解析：[] 是空列表；{} 是空字典，() 是空元组。

### 23. 循环

题目：for 循环常搭配哪个函数生成连续数字序列？

- A. range
- B. len
- C. input
- D. type

答案：A

解析：range 生成连续整数序列，如 range(5) 是 0,1,2,3,4。

### 24. Python基础

题目：Python 源程序文件的后缀名是？

- A. .py
- B. .python
- C. .pt
- D. .pic

答案：A

解析：Python 源程序文件以 .py 为后缀。

### 25. 类型转换

题目：list('abc') 的结果是？

- A. ['a', 'b', 'c']
- B. 'abc'
- C. ['abc']
- D. 报错

答案：A

解析：list() 把可迭代对象拆成元素列表，字符串会拆成单个字符。

### 26. 输出

题目：Python 中把内容输出到控制台用哪个函数？

- A. print()
- B. input()
- C. output()
- D. show()

答案：A

解析：print() 负责输出显示；input() 是读取键盘输入。

### 27. 运算符

题目：判断整数 num 是否为偶数，正确的条件是？

- A. num % 2 == 0
- B. num / 2 == 0
- C. num // 2 == 0
- D. num % 2 == 1

答案：A

解析：偶数除以 2 余 0，用 num % 2 == 0；== 1 判断的是奇数。

### 28. 内置函数

题目：len('study') 的返回值是？

- A. 4
- B. 5
- C. 6
- D. 0

答案：B

解析：len() 返回字符个数，'study' 共 5 个字母。

### 29. 输入处理

题目：读入一个整数并能直接参与数学运算，正确写法是？

- A. int(input())
- B. input()
- C. float()
- D. str(input())

答案：A

解析：input() 读到的永远是字符串，要做整数运算得用 int(input()) 转换。

### 30. 数据类型

题目：下列属于浮点数（float）类型的是？

- A. 2.56
- B. 99
- C. -10
- D. False

答案：A

解析：带小数点的是 float；99、-10 是 int，False 是 bool。

### 31. 数据类型

题目：下列哪一个是整型 int？

- A. 88
- B. 3.14
- C. '66'
- D. True

答案：A

解析：88 是整数 int；3.14 是 float，'66' 是 str，True 是 bool。

### 32. 运算符

题目：print(3 + 4 * 2) 的结果是？

- A. 11
- B. 14
- C. 7
- D. 9

答案：A

解析：先乘除后加减：4*2=8，再 3+8=11。

### 33. 字符串运算符

题目：print('5' + '3') 的输出是？

- A. 53
- B. 8
- C. 报错
- D. 5 3

答案：A

解析：引号里是字符串，+ 做拼接得到 '53'，不是数字相加。

### 34. 运算符

题目：表示“大于等于”的运算符是？

- A. >=
- B. >
- C. <
- D. =>

答案：A

解析：>= 表示大于等于；单个 = 是赋值，=> 在 Python 里不合法。

### 35. 运算符

题目：a = 5，想让 a 增加 3，下列哪种写法是错误的（a 其实没变）？

- A. a + 3
- B. a += 3
- C. a = a + 3
- D. a = 3 + a

答案：A

解析：a + 3 只算出 8 却没赋回去，a 仍是 5；要写 a += 3 或 a = a + 3。

### 36. 变量与赋值

题目：同时给 a 赋 1、b 赋 2，正确的简写是？

- A. a, b = 1, 2
- B. a = 1 b = 2
- C. a := 1, b := 2
- D. a; b = 1; 2

答案：A

解析：一行多赋值写成 a, b = 1, 2；写成 a=1 b=2 缺少分隔会报错。

### 37. 运算符

题目：判断两个值是否相等，使用哪个运算符？

- A. ==
- B. =
- C. ===
- D. =>

答案：A

解析：== 比较是否相等；单个 = 是赋值，Python 没有 ===。

### 38. 布尔判断

题目：下列哪个表达式的结果为 False？

- A. 10 < 5
- B. 5 > 2
- C. 3 == 3
- D. 2 != 3

答案：A

解析：10 < 5 不成立，结果 False；其余三项都为 True。

### 39. 流程控制

题目：当条件成立时反复执行，使用哪个关键字？

- A. while
- B. if
- C. def
- D. return

答案：A

解析：while 在条件为真时反复执行；for 用于遍历序列。

### 40. 类型转换

题目：print(int(7.9)) 的输出是？

- A. 7
- B. 8
- C. 7.9
- D. 报错

答案：A

解析：int() 直接截掉小数部分（向 0 取整），不是四舍五入，所以是 7。

### 41. 布尔判断

题目：not False 的结果是？

- A. True
- B. False
- C. 0
- D. None

答案：A

解析：not 取反，False 取反就是 True。

### 42. 布尔值

题目：bool('abc') 的结果是？

- A. True
- B. False
- C. 'abc'
- D. 0

答案：A

解析：非空字符串为真，bool('abc') 是 True；只有空串 '' 才是 False。

### 43. 布尔值

题目：下列转成 bool 后结果为 True 的是？

- A. bool(-5)
- B. bool('')
- C. bool([])
- D. bool(0)

答案：A

解析：只有 0、空字符串、空容器为假；-5 非 0，bool(-5) 为 True。

### 44. 流程控制

题目：list(range(2, 7)) 的结果是？

- A. [2, 3, 4, 5, 6]
- B. [2, 3, 4, 5, 6, 7]
- C. [0, 1, 2, 3, 4, 5, 6]
- D. [2, 7]

答案：A

解析：range(2, 7) 含 2 不含 7，得到 2、3、4、5、6。

### 45. 布尔值

题目：bool(set()) 的结果是？

- A. False
- B. True
- C. set()
- D. 1

答案：A

解析：空集合视为假，bool(set()) 为 False；非空集合才是 True。

## 普通模式

### 46. 流程控制

题目：range(2, 8, 2) 依次产生哪些整数？

- A. 2, 4, 6
- B. 2, 4, 6, 8
- C. 2, 3, 4, 5, 6, 7
- D. 2, 8

答案：A

解析：range(start, stop, step) 含 start 不含 stop，步长 2：2, 4, 6（8 取不到）。

### 47. 输入处理

题目：'3,5,7'.split(',') 的结果是？

- A. [3, 5, 7]
- B. ['3', '5', '7']
- C. '3 5 7'
- D. (3, 5, 7)

答案：B

解析：split 切成字符串列表，元素仍是字符串！要参与计算得先用 int() 或 float() 转换——输入处理最常见的坑。

### 48. 列表推导式

题目：[i * i for i in range(1, 4)] 的结果是？

- A. [1, 4, 9]
- B. [1, 2, 3]
- C. [1, 4, 9, 16]
- D. [0, 1, 4]

答案：A

解析：列表生成式对 1, 2, 3 各求平方得 1, 4, 9；range(1, 4) 不含 4。

### 49. 列表

题目：lst = [1, 2, 3]; lst.append([4, 5]) 之后，lst 是？

- A. [1, 2, 3, 4, 5]
- B. [1, 2, 3, [4, 5]]
- C. [1, 2, 3, 4]
- D. 报错

答案：B

解析：append 把参数当成一个元素整体加进去，所以末尾是一个子列表；要展开成 4、5 得用 extend 或 +。

### 50. 字典

题目：d = {'a': 1, 'b': 2}，下面哪个会触发 KeyError？

- A. d['a']
- B. d['c']
- C. d.get('c')
- D. len(d)

答案：B

解析：键 'c' 不存在，用中括号取值会 KeyError；而 d.get('c') 返回 None，不报错。

### 51. 循环

题目：for i, v in enumerate(['a', 'b', 'c']): 第一次循环时 i 和 v 分别是？

- A. 1 和 'a'
- B. 0 和 'a'
- C. 0 和 'b'
- D. 'a' 和 0

答案：B

解析：enumerate 默认从 0 开始配下标，第一次 i = 0、v = 'a'。

### 52. 函数式

题目：list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])) 的结果是？

- A. [1, 3]
- B. [2, 4]
- C. [1, 2, 3, 4]
- D. [True, False, True, False]

答案：B

解析：filter 只保留让 lambda 返回 True 的元素，这里是偶数 2 和 4。

### 53. 集合

题目：set([1, 2, 2, 3, 3, 3]) 的结果是？

- A. [1, 2, 3]
- B. {1, 2, 3}
- C. {1, 2, 2, 3, 3, 3}
- D. (1, 2, 3)

答案：B

解析：集合 set 自动去重，用花括号表示；常用来判断元素是否已经出现过。

### 54. 统计

题目：lst = [3, 9, 2, 8]，sum(1 for v in lst if v > 5) 的结果是？

- A. 2
- B. 17
- C. 4
- D. 5

答案：A

解析：对每个大于 5 的元素计 1 再求和，相当于统计个数：9 和 8 共 2 个。

### 55. 文件与异常

题目：用 open() 打开一个不存在的文件，应该捕获哪个异常？

- A. ValueError
- B. FileNotFoundError
- C. KeyError
- D. TypeError

答案：B

解析：文件不存在时抛 FileNotFoundError，用 try ... except FileNotFoundError 捕获并友好提示。

### 56. 代码填空·输入处理

题目：键盘输入用逗号分隔的数字，求它们的和。____ 处应填什么？

```python
s = input("输入数字，用逗号分隔：")   # 例如 "3,5,7"
nums = [____ for v in s.split(",")]
print(sum(nums))                      # 期望输出 15
```

- A. float(v)
- B. v
- C. str(v)
- D. v.split()

答案：A

解析：split 得到的是字符串列表，必须先转成数字（float 或 int）才能求和；直接对字符串求和会报错。

### 57. 代码填空·文件异常

题目：打开文件读取内容，文件不存在时给出提示。except 后的 ____ 应填什么？

```python
try:
    with open("data.txt", "r", encoding="utf-8") as f:
        print(f.read())
except ____:
    print("文件不存在！")
```

- A. FileNotFoundError
- B. ValueError
- C. KeyError
- D. IndexError

答案：A

解析：文件不存在时抛出的是 FileNotFoundError，捕获它即可给出友好提示。

### 58. 代码填空·循环累加

题目：用循环累加 1 到 100 的和。range 的 ____ 应填什么？

```python
s = 0
for i in range(____):
    s += i
print(s)        # 期望输出 5050
```

- A. 1, 101
- B. 1, 100
- C. 0, 100
- D. 1, 99

答案：A

解析：range 右端点取不到，要累加到 100 必须写 range(1, 101)。

### 59. 代码填空·去重保序

题目：去除列表重复元素并保留原始顺序。if 后的 ____ 应填什么？

```python
def remove_duplicates(lst):
    seen = set()
    result = []
    for item in lst:
        if ____:
            seen.add(item)
            result.append(item)
    return result
```

- A. item not in seen
- B. item in seen
- C. item not in result
- D. item == seen

答案：A

解析：只有没见过的元素才加入结果；用 set 的 not in 判断高效，既去重又保留顺序。

### 60. 随机模块

题目：random.random() 生成的随机数范围是？

- A. [0, 1)
- B. [0, 1]
- C. (0, 1)
- D. [1, 10]

答案：A

解析：random.random() 返回 0（含）到 1（不含）之间的浮点数。

### 61. 集合

题目：创建一个空集合，正确的是？

- A. s = set()
- B. s = {}
- C. s = []
- D. s = ()

答案：A

解析：{} 创建的是空字典；空集合只能用 set()。

### 62. 文件操作

题目：open 的模式 'a' 表示？

- A. 追加写入（append）
- B. 只读
- C. 二进制
- D. 新建并覆盖

答案：A

解析：a = append 追加，从文件末尾写；r=read, w=write, b=binary。

### 63. 文件操作

题目：open 的模式 'w' 的特点是？

- A. 覆盖写入（文件已存在则先清空）
- B. 只读不能写
- C. 追加到末尾
- D. 文件不存在会报错

答案：A

解析：w 模式打开会清空原内容再写；要保留旧内容追加得用 a。

### 64. 可变性

题目：下列哪个是不可变（immutable）数据类型？

- A. 元组
- B. 列表
- C. 字典
- D. 集合

答案：A

解析：不可变：数值、字符串、元组；可变：列表、字典、集合。

### 65. 字典

题目：字典的键（key）必须满足什么？

- A. 不可变且唯一
- B. 可以重复
- C. 必须是字符串
- D. 必须是数字

答案：A

解析：键必须是不可变类型（字符串、数字、元组）且唯一，值可以是任意类型。

### 66. 列表方法

题目：lst.pop() 不带参数时的行为是？

- A. 移除并返回最后一个元素
- B. 移除第一个元素
- C. 清空列表
- D. 返回列表长度

答案：A

解析：pop() 默认移除并返回最后一个元素；pop(i) 移除指定位置。

### 67. 列表方法

题目：lst.insert(0, 5) 的作用是？

- A. 在索引 0 处插入 5
- B. 在末尾添加 5
- C. 删除索引 0 的元素
- D. 把第 0 个改成 5

答案：A

解析：insert(位置, 元素) 在指定索引处插入，后面的元素依次后移。

### 68. 字符串方法

题目：'-'.join(['a', 'b', 'c']) 的结果是？

- A. 'a-b-c'
- B. ['a', 'b', 'c']
- C. 'abc'
- D. 'a,b,c'

答案：A

解析：join 用分隔符把可迭代对象的元素连成一个字符串。

### 69. 字符串方法

题目：'  hi  '.strip() 的结果是？

- A. 'hi'
- B. '  hi'
- C. 'hi  '
- D. '  hi  '

答案：A

解析：strip() 去掉字符串首尾的空白字符。

### 70. 字符串方法

题目：'123'.isdigit() 的结果是？

- A. True
- B. False
- C. 123
- D. 报错

答案：A

解析：isdigit() 判断字符串是否全是数字，全数字返回 True。

### 71. 字符串格式化

题目：'{} 和 {}'.format('A', 'B') 的结果是？

- A. 'A 和 B'
- B. '{} 和 {}'
- C. 'AB'
- D. 报错

答案：A

解析：format 按顺序把 {} 替换成参数。

### 72. 字符串运算符

题目：'ab' * 3 的结果是？

- A. 'ababab'
- B. 'ab3'
- C. 'aaabbb'
- D. 报错

答案：A

解析：字符串 * 整数 表示复制重复，'ab' 重复 3 次。

### 73. 字符串运算符

题目：'H' in 'Hello' 的结果是？

- A. True
- B. False
- C. 0
- D. 'H'

答案：A

解析：in 判断子串是否存在，'H' 在 'Hello' 中，返回 True。

### 74. 列表与元组

题目：列表和元组最本质的区别是？

- A. 列表可变、元组不可变
- B. 列表用圆括号、元组用方括号
- C. 元组能存数字、列表不能
- D. 没有区别

答案：A

解析：本质区别是可变性：列表 [] 可增删改，元组 () 一旦创建不能改。

### 75. 切片

题目：lst = [0, 1, 2, 3, 4]，lst[1:4] 的结果是？

- A. [1, 2, 3]
- B. [1, 2, 3, 4]
- C. [0, 1, 2, 3]
- D. [2, 3, 4]

答案：A

解析：切片 [1:4] 含起点 1 不含终点 4，取索引 1、2、3。

### 76. 切片

题目：lst = [0, 1, 2, 3, 4, 5]，lst[::2] 的结果是？

- A. [0, 2, 4]
- B. [1, 3, 5]
- C. [0, 1, 2]
- D. [5, 3, 1]

答案：A

解析：[::2] 步长为 2，从头每隔一个取，得到索引 0、2、4。

### 77. 列表方法

题目：lst = [1, 2]; lst.extend([3, 4]) 后，lst 是？

- A. [1, 2, 3, 4]
- B. [1, 2, [3, 4]]
- C. [3, 4, 1, 2]
- D. [1, 2]

答案：A

解析：extend 把另一个列表的元素逐个加到末尾；append([3,4]) 才会变嵌套。

### 78. 字符串方法

题目：'aXbXc'.replace('X', '-') 的结果是？

- A. 'a-b-c'
- B. 'aXbXc'
- C. 'abc'
- D. 'a-bXc'

答案：A

解析：replace 把所有匹配的子串都替换掉。

### 79. 字符串方法

题目：'abcabc'.count('a') 的结果是？

- A. 2
- B. 1
- C. 3
- D. 0

答案：A

解析：count 统计子串出现次数，'a' 出现 2 次。

### 80. 字符串方法

题目：'hello'.find('l') 的结果是？

- A. 2
- B. 3
- C. -1
- D. 1

答案：A

解析：find 返回子串第一次出现的下标，第一个 'l' 在索引 2；找不到返回 -1。

### 81. 字符串方法

题目：'hello world'.title() 的结果是？

- A. 'Hello World'
- B. 'HELLO WORLD'
- C. 'Hello world'
- D. 'hello world'

答案：A

解析：title() 把每个单词的首字母都变大写。

### 82. 字符串方法

题目：'python'.startswith('py') 的结果是？

- A. True
- B. False
- C. 'py'
- D. 报错

答案：A

解析：startswith 判断字符串是否以指定前缀开头，返回布尔值。

### 83. 循环else

题目：for 循环正常结束（没有被 break 中断）时，会执行 else 子句吗？

- A. 会执行
- B. 不会执行
- C. 报错
- D. 随机决定

答案：A

解析：循环正常跑完会执行 else；若中途 break 退出，则不执行 else。

### 84. 模块导入

题目：import math 之后，调用平方根函数要写成？

- A. math.sqrt(x)
- B. sqrt(x)
- C. math(sqrt(x))
- D. import sqrt

答案：A

解析：import math 要用 模块名.函数名；想直接写 sqrt() 得用 from math import sqrt。

### 85. 类型转换

题目：tuple([1, 2, 3]) 的结果是？

- A. (1, 2, 3)
- B. [1, 2, 3]
- C. {1, 2, 3}
- D. 报错

答案：A

解析：tuple() 把可迭代对象转成元组。

### 86. 字符串方法

题目：'apple'.replace('p', 'm') 的结果是？

- A. ammle
- B. amle
- C. ample
- D. aMMle

答案：A

解析：replace 替换全部匹配字符，'apple' 里两个 p 都变 m → 'ammle'。

### 87. 字典

题目：dic = {'name': 'Tom', 'age': 18}，删除键 'age' 的正确语句是？

- A. del dic['age']
- B. del dic(age)
- C. remove dic['age']
- D. dic.popitem()

答案：A

解析：del 字典[键] 删除指定键值对；popitem() 删的是末尾一项，不一定是 age。

### 88. 复合赋值

题目：a = 5; a *= 3，执行后 a 的值是？

- A. 15
- B. 5
- C. 8
- D. 53

答案：A

解析：a *= 3 等价于 a = a * 3，5×3 = 15。

### 89. 布尔判断

题目：下列表达式结果为 False 的是？

- A. 7 >= 7 and ''
- B. 10 > 2 or 3 < 1
- C. not 0
- D. bool([1, 2])

答案：A

解析：and 要两边都真；7>=7 为真但 '' 为假，结果取 '' 即假。其余都为 True。

### 90. 流程控制

题目：list(range(-2, 3)) 的结果是？

- A. [-2, -1, 0, 1, 2]
- B. [-2, -1, 0, 1, 2, 3]
- C. [0, 1, 2]
- D. [-2, -1]

答案：A

解析：range 可从负数开始，含 -2 不含 3，得到 -2,-1,0,1,2。

### 91. 代码阅读·条件

题目：阅读代码，输出是什么？

```python
score = 75
if score >= 90:
    print("A")
elif score >= 60:
    print("B")
else:
    print("C")
```

- A. B
- B. A
- C. C
- D. 无输出

答案：A

解析：75 不满足 >=90，但满足 >=60，走 elif 分支输出 B。

### 92. 切片

题目：lst = [10, 20, 30, 40, 50]，lst[1:-2] 的结果是？

- A. [20, 30]
- B. [20, 30, 40]
- C. [10, 20, 30]
- D. [30, 40]

答案：A

解析：-2 指倒数第 2 个（40）的位置，切片不含末端，取索引 1、2 → [20, 30]。

### 93. 函数

题目：定义一个无参数、无返回值的函数，正确写法是？

- A. def func(): pass
- B. function func():
- C. func = def()
- D. define func()

答案：A

解析：用 def 定义函数，函数体可用 pass 占位表示暂时什么都不做。

### 94. 运算符

题目：15 // 2 和 15 / 2 的结果分别是？

- A. 7 和 7.5
- B. 7.0 和 7
- C. 7.5 和 7
- D. 8 和 7.5

答案：A

解析：// 整除得整数 7；/ 是真除法得浮点 7.5。

### 95. 代码阅读·循环

题目：阅读代码，打印出的数字是？

```python
for i in range(4):
    if i == 2:
        continue
    print(i)
```

- A. 0 1 3
- B. 0 1 2 3
- C. 2
- D. 0 1

答案：A

解析：i==2 时 continue 跳过本次 print，所以打印 0、1、3。

### 96. 类型转换

题目：下列哪行代码运行会直接报错？

- A. int('3.14')
- B. int('66')
- C. float('9.9')
- D. str(123)

答案：A

解析：int() 不能直接转带小数点的字符串，int('3.14') 抛 ValueError；需先 float('3.14')。

### 97. 运算符

题目：表达式 3 > 1 or 5 < 0 and 2 == 2 的结果是？

- A. True
- B. False
- C. 0
- D. 报错

答案：A

解析：and 优先级高于 or：先算 5<0 and 2==2 为 False，再 True or False 为 True。

### 98. 代码阅读·循环

题目：阅读代码，最终打印 i 的值是？

```python
i = 0
while i < 3:
    i += 1
print(i)
```

- A. 3
- B. 0
- C. 2
- D. 4

答案：A

解析：i 从 0 每次 +1，到 3 时 3<3 不成立退出，print(i) 输出 3。

### 99. 代码阅读·循环

题目：阅读代码，输出结果是？

```python
for i in range(10):
    if i == 4:
        break
print(i)
```

- A. 4
- B. 3
- C. 9
- D. 10

答案：A

解析：i 到 4 时 break 跳出循环，此时 i 已是 4，print 输出 4。

### 100. 列表方法

题目：lst = [10, 20, 30]，执行 lst.pop(1) 的返回值和列表变化是？

- A. 返回 20，列表变为 [10, 30]
- B. 返回 10，列表变为 [20, 30]
- C. 返回 30，删除末尾元素
- D. 报错，pop 不能传参数

答案：A

解析：pop(1) 删除并返回索引 1 的元素 20，列表剩 [10, 30]。

### 101. 字符串

题目：下列哪种写法可以表示跨多行的字符串？

- A. 用三引号包裹
- B. 用单引号包裹
- C. 用 + 连接
- D. 用逗号分隔

答案：A

解析：三引号 ''' ''' 或三个双引号可写多行字符串；单引号、双引号只能写单行。

### 102. 代码阅读·函数

题目：阅读代码，打印结果是？

```python
def add(x, y):
    return x + y

res = add(2, 5)
print(res)
```

- A. 7
- B. 25
- C. '7'
- D. None

答案：A

解析：函数返回 2+5=7，print 输出整数 7（不是字符串 '7'）。

### 103. 集合

题目：关于集合 set，下列描述错误的是？

- A. 可以通过 set[0] 取值
- B. 元素不允许重复
- C. 元素无序
- D. 用 {} 或 set() 创建

答案：A

解析：集合无序、不支持下标，set[0] 会报错；它确实自动去重、无序。

### 104. 字符串格式化

题目：name = '小李'，要用 f-string 输出“我的名字：小李”，正确写法是？

- A. print(f"我的名字：{name}")
- B. print("我的名字：{name}")
- C. print(f"我的名字：name")
- D. print("我的名字：" + name)

答案：A

解析：f-string 要加前缀 f 且变量放进 {} 中：f"我的名字：{name}"。

### 105. 列表方法

题目：lst = [1, 3, 5]，执行 lst.append([7, 9]) 后列表长度为？

- A. 4
- B. 3
- C. 5
- D. 6

答案：A

解析：append 把 [7, 9] 当成一个元素加入，长度从 3 变 4（最后一项是子列表）。

### 106. 元组

题目：t = (8)，print(type(t)) 显示的类型是？

- A. int
- B. tuple
- C. list
- D. str

答案：A

解析：(8) 只是加了括号的整数；单元素元组必须写成 (8,) 带逗号才是 tuple。

### 107. 列表方法

题目：要删除列表中下标为 3 的元素，正确操作是？

- A. del lst[3]
- B. lst.remove(3)
- C. lst.pop()
- D. clear(lst[3])

答案：A

解析：del lst[3] 按下标删；remove(3) 删的是“值为 3”的元素，含义不同。

### 108. 字典

题目：d = {'a': 1, 'b': 2}，d.items() 返回的是？

- A. 键值对组合
- B. 所有键
- C. 所有值
- D. 字典长度

答案：A

解析：items() 返回 (键, 值) 形式的键值对；keys() 只返回键，values() 只返回值。

### 109. 字典

题目：只遍历字典中所有的“键”，使用哪个方法？

- A. d.keys()
- B. d.values()
- C. d.items()
- D. d.all()

答案：A

解析：keys() 返回所有键；values() 返回所有值，items() 返回键值对。

### 110. 循环

题目：for i in range(len(lst)) 这种写法常用来做什么？

- A. 获取列表下标
- B. 直接取列表值
- C. 生成随机数
- D. 反转字符串

答案：A

解析：range(len(lst)) 生成 0..len-1，即每个元素的下标，再用 lst[i] 取值。

### 111. 字典

题目：判断键 key 是否在字典 d 中，正确写法是？

- A. if key in d:
- B. if key == d:
- C. if key of d:
- D. if key is d:

答案：A

解析：in 判断键是否存在，可避免直接取值触发 KeyError。

### 112. 内置函数

题目：max([3, 9, 1, 5]) 的返回值是？

- A. 9
- B. 1
- C. 3
- D. 5

答案：A

解析：max() 返回最大值 9；对应 min() 返回最小值。

### 113. 内置函数

题目：min([3, 9, 1, 5]) 的返回值是？

- A. 1
- B. 9
- C. 3
- D. 5

答案：A

解析：min() 返回最小值 1。

### 114. 列表推导式

题目：[x * 3 for x in range(4)] 的结果是？

- A. [0, 3, 6, 9]
- B. [1, 3, 6, 9]
- C. [3, 6, 9, 12]
- D. [0, 1, 2, 3]

答案：A

解析：range(4) 是 0,1,2,3，各乘 3 得 [0, 3, 6, 9]。

### 115. 列表推导式

题目：生成 1~10 之间的偶数列表，正确的推导式是？

- A. [x for x in range(1, 11) if x % 2 == 0]
- B. [x for x in range(1, 11) if x % 2 == 1]
- C. [x * 2 for x in range(10)]
- D. [x for x in range(1, 10)]

答案：A

解析：加 if x%2==0 过滤偶数，range(1,11) 取 1~10，得到 [2, 4, 6, 8, 10]。

### 116. 语法

题目：pass 语句的作用是？

- A. 占位，什么都不做
- B. 终止循环
- C. 返回数值
- D. 跳过本次循环

答案：A

解析：pass 是空操作占位符，用在语法上需要语句但暂时不写内容的地方。

### 117. 字符串方法

题目：'python'.upper() 的结果是？

- A. PYTHON
- B. python
- C. Python
- D. PyThOn

答案：A

解析：upper() 把字母全部转大写；lower() 转小写。

### 118. 内置函数

题目：round(4.62, 1) 的结果是？

- A. 4.6
- B. 4.7
- C. 5
- D. 4

答案：A

解析：round(x, 1) 保留 1 位小数，4.62 → 4.6。

### 119. 集合

题目：集合 set 的核心特点是？

- A. 无序且元素不重复
- B. 有序且允许重复
- C. 支持下标索引取值
- D. 只能存放数字

答案：A

解析：集合自动去重且无序，不能用下标取值。

### 120. 列表方法

题目：lst = [5, 1, 3]，执行 lst.sort() 后列表是？

- A. [1, 3, 5]
- B. [5, 3, 1]
- C. [5, 1, 3]
- D. 不变

答案：A

解析：sort() 原地升序排序，列表变为 [1, 3, 5]，返回值是 None。

### 121. 字符串方法

题目：不带参数的 split() 默认按什么分割字符串？

- A. 空格（空白字符）
- B. 逗号
- C. 换行
- D. 句号

答案：A

解析：不带参数的 split() 按任意空白（空格/制表/换行）切分。

### 122. 函数

题目：在函数内部修改全局变量，需要先用哪个关键字声明？

- A. global
- B. local
- C. var
- D. static

答案：A

解析：要在函数里给全局变量重新赋值，得先写 global 变量名，否则会被当成局部变量。

### 123. 输出

题目：print('test', end='') 与默认 print 相比，不会自动添加什么？

- A. 换行符 \n
- B. 空格
- C. 逗号
- D. 制表符 \t

答案：A

解析：print 默认在末尾加换行符；end='' 取消换行，让下次输出接在同一行。

## 挑战模式

### 124. 排序

题目：选择排序每一轮的核心操作是？

- A. 相邻两个元素比较并不断交换
- B. 在未排序部分找到最小值，放到当前位置
- C. 随机交换两个元素
- D. 把整个列表反转一次

答案：B

解析：选择排序每轮在内层找到最小值的下标，内层结束后只交换一次；相邻比较、内层反复交换的是冒泡排序。

### 125. 排序

题目：选择排序中，交换元素的语句应该写在哪里？

- A. 写在内层 if 里面
- B. 写在内层 for 循环外面（外层 for 之内）
- C. 写在所有循环外面
- D. 选择排序不需要交换

答案：B

解析：内层只更新最小值下标 min_idx，内层结束后才交换一次。写进 if 里就变成冒泡了——这是最常考的区别。

### 126. 二维列表

题目：执行 m = [[0]*3]*2; m[0][0] = 1 之后，m 是？

- A. [[1, 0, 0], [0, 0, 0]]
- B. [[1, 0, 0], [1, 0, 0]]
- C. [[1, 1, 1], [0, 0, 0]]
- D. 报错

答案：B

解析：[[0]*3]*2 让两行指向同一个列表，改一行另一行也跟着变！正确写法是 [[0]*3 for _ in range(2)]。

### 127. 二维列表

题目：一个 2 行 4 列的矩阵，转置后变成几行几列？

- A. 2 行 4 列
- B. 4 行 2 列
- C. 4 行 4 列
- D. 2 行 2 列

答案：B

解析：转置把行列互换：原 m[i][j] 变成新 result[j][i]，所以 2×4 变 4×2。

### 128. 二维列表

题目：n×n 矩阵 matrix 的主对角线元素下标是？

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[0][i]
- D. matrix[i][0]

答案：A

解析：主对角线行号等于列号，即 matrix[i][i]；副对角线才是 matrix[i][n-1-i]。

### 129. 二维列表

题目：n×n 矩阵的副对角线（右上到左下）元素下标是？

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[n-i][i]
- D. matrix[i][n+i]

答案：B

解析：副对角线满足 行号 + 列号 = n-1，所以列下标是 n-1-i。

### 130. 数论算法

题目：辗转相除法 while b: a, b = b, a % b; return a，求 gcd(12, 18) 的结果是？

- A. 2
- B. 3
- C. 6
- D. 36

答案：C

解析：(12,18)→(18,12)→(12,6)→(6,0)，b 为 0 时返回 a = 6。12 与 18 的最大公约数是 6。

### 131. 数论算法

题目：已知 gcd(a, b)，求最小公倍数 lcm 的正确写法是？

- A. a * b / gcd(a, b)
- B. a * b // gcd(a, b)
- C. a + b - gcd(a, b)
- D. gcd(a, b) // (a * b)

答案：B

解析：lcm = a*b/gcd，要用整除 // 得到整数；用 / 会得到带小数的 float。

### 132. 参数传递

题目：def f(lst): lst.append(0)。执行 a = [1, 2]; f(a) 之后，a 是？

- A. [1, 2]
- B. [1, 2, 0]
- C. [0, 1, 2]
- D. None

答案：B

解析：列表是可变对象，函数内对它 append 会直接改到原列表；而整数等不可变对象不会被函数改变。

### 133. 参数传递

题目：def f(x): x += 1; return x。执行 a = 5; b = f(a) 之后，a 和 b 分别是？

- A. 5 和 6
- B. 6 和 6
- C. 5 和 5
- D. 6 和 5

答案：A

解析：整数不可变，函数内 x += 1 只改局部副本并返回 6，原 a 仍是 5。可与上一题的列表对照记忆。

### 134. 递归

题目：递归判断素数 def is_prime(num, i=2): 中，返回 True 的终止条件通常是？

- A. num == 0
- B. i * i > num
- C. i > num
- D. num % i == 0

答案：B

解析：当 i 的平方已超过 num，说明没找到能整除的因子，num 是素数，返回 True；只需查到 √num 即可。

### 135. 去重算法

题目：要给列表去重并保留原始顺序，用哪种结构记录“已出现过”最高效？

- A. 再开一个 list，每次用 in 查找
- B. 用 set 记录已出现的元素
- C. 把所有元素拼成字符串
- D. 用 sort 排序后再比较

答案：B

解析：set 的成员判断 in 接近 O(1)，比在 list 里查找快；遍历时没见过就加入结果并记进 set，既去重又保序。

### 136. 代码填空·选择排序

题目：选择排序（升序）。内层找最小值的判断 ____ 应填什么？

```python
def selection_sort(arr):
    n = len(arr)
    for i in range(n - 1):
        min_idx = i
        for j in range(i + 1, n):
            if ____:          # 找更小的
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr
```

- A. arr[j] < arr[min_idx]
- B. arr[j] > arr[min_idx]
- C. arr[j] < arr[i]
- D. j < min_idx

答案：A

解析：要在未排序区找最小值，需比较 arr[j] 与当前最小 arr[min_idx]；写成 > 会变降序，比较 arr[i] 是常见错误。

### 137. 代码填空·辗转相除

题目：辗转相除法求最大公约数。循环体内 ____ 应填什么？

```python
def gcd(a, b):
    while b != 0:
        a, b = ____
    return a
```

- A. b, a % b
- B. a % b, b
- C. b, a // b
- D. a, b % a

答案：A

解析：新的 a 取旧 b，新的 b 取 a 除以 b 的余数，直到 b 为 0，此时 a 即最大公约数。

### 138. 代码填空·矩阵转置

题目：矩阵转置，把原矩阵行列互换。____ 处应填什么？

```python
def transpose(matrix):
    rows = len(matrix)
    cols = len(matrix[0])
    result = [[0] * rows for _ in range(cols)]
    for i in range(rows):
        for j in range(cols):
            ____ = matrix[i][j]
    return result
```

- A. result[j][i]
- B. result[i][j]
- C. result[i][i]
- D. matrix[j][i]

答案：A

解析：转置就是行列下标互换：原 matrix[i][j] 放到新 result[j][i]。

### 139. 代码填空·二维列表初始化

题目：创建 3 行 4 列、初值全 0 且各行互相独立的二维列表。____ 应填什么？

```python
rows, cols = 3, 4
matrix = ____
matrix[0][0] = 1
print(matrix)   # 期望只改到第 0 行
```

- A. [[0]*cols for _ in range(rows)]
- B. [[0]*cols]*rows
- C. [0]*cols*rows
- D. [[0]*rows]*cols

答案：A

解析：[[0]*cols]*rows 会让各行共享同一引用，改一行全变；必须用列表推导式生成相互独立的行。

### 140. 代码填空·递归素数

题目：递归判断 num 是否为素数。返回 True 的终止条件 ____ 应填什么？

```python
def is_prime(num, i=2):
    if num < 2:
        return False
    if ____:
        return True
    if num % i == 0:
        return False
    return is_prime(num, i + 1)
```

- A. i * i > num
- B. i > num
- C. num % i == 0
- D. i == num

答案：A

解析：试除到 √num 即可；当 i*i > num 仍没找到因子，说明 num 是素数，返回 True。

### 141. 代码填空·主对角线

题目：求 n×n 矩阵主对角线元素之和。____ 应填什么？

```python
def diag_sum(matrix):
    n = len(matrix)
    s = 0
    for i in range(n):
        s += ____
    return s
```

- A. matrix[i][i]
- B. matrix[i][n-1-i]
- C. matrix[0][i]
- D. matrix[i][0]

答案：A

解析：主对角线行号等于列号，累加 matrix[i][i]；若要副对角线则是 matrix[i][n-1-i]。

### 142. 代码填空·lambda 过滤

题目：用 lambda + filter 删除列表里所有的 'A'。____ 应填什么？

```python
lst = list("ABCAAD")
result = list(filter(____, lst))
print("".join(result))   # 期望输出 BCD
```

- A. lambda x: x != 'A'
- B. lambda x: x == 'A'
- C. lambda x: 'A'
- D. x != 'A'

答案：A

解析：filter 保留让函数返回 True 的元素；要删掉 'A'，就保留 x != 'A' 的元素。

### 143. 排序　〔组员5 制作〕

题目：冒泡排序按升序排列时，第一轮完整比较结束后，哪个元素通常会被放到列表末尾？

- A. 当前最大值
- B. 当前最小值
- C. 第一个元素
- D. 随机元素

答案：A

解析：升序冒泡排序每轮不断交换相邻逆序元素，第一轮会把当前最大值一路“冒泡”到末尾。

### 144. 递归　〔组员5 制作〕

题目：递归函数最容易出错的地方是什么？

- A. 没有终止条件或没有向终止条件靠近
- B. 函数名太短
- C. 用了 return
- D. 参数数量超过 1 个

答案：A

解析：递归必须有明确终止条件，并且每次递归调用都要让问题规模变小，否则可能无限递归直到 RecursionError。

### 145. 二维列表　〔组员5 制作〕

题目：复制二维列表时，matrix[:] 的特点是？

- A. 只复制外层列表，内层列表仍共享
- B. 能完整深拷贝所有层
- C. 会把二维列表转成一维列表
- D. 一定会报错

答案：A

解析：matrix[:] 是浅拷贝，只新建外层列表；里面每一行仍然指向原来的列表，修改某一行内容会互相影响。

### 146. 字典排序　〔组员5 制作〕

题目：sorted(d.items(), key=lambda item: item[1]) 的排序依据是？

- A. 按字典的值排序
- B. 按字典的键排序
- C. 按键值对数量排序
- D. 按插入顺序反转

答案：A

解析：d.items() 产生 (键, 值) 形式的元组，item[1] 取的是值，所以会按字典的值排序。

### 147. 文件与异常　〔组员5 制作〕

题目：try...except...else...finally 中，finally 代码块什么时候执行？

- A. 无论是否发生异常都会执行
- B. 只有发生异常才执行
- C. 只有没有异常才执行
- D. 只有 return 后才执行

答案：A

解析：finally 常用于关闭文件、释放资源等收尾操作，不管 try 中是否出错，通常都会执行。

### 148. 代码阅读·默认参数　〔组员5 制作〕

题目：阅读代码，程序两次 print 的输出分别是什么？

```python
def collect(x, box=[]):
    box.append(x)
    return box

print(collect(1))
print(collect(2))
```

- A. [1] 和 [2]
- B. [1] 和 [1, 2]
- C. [1, 2] 和 [1, 2]
- D. 报错

答案：B

解析：默认参数中的列表只会在函数定义时创建一次，两次调用共用同一个 box，所以第二次输出 [1, 2]。

### 149. 代码阅读·浅拷贝　〔组员5 制作〕

题目：阅读代码，print(a) 的输出是什么？

```python
a = [[1], [2]]
b = a[:]
b[0].append(9)
print(a)
```

- A. [[1], [2]]
- B. [[1, 9], [2]]
- C. [[1], [2, 9]]
- D. [[9], [2]]

答案：B

解析：a[:] 只复制外层列表，b[0] 和 a[0] 指向同一个内层列表；append(9) 会影响 a[0]。

### 150. 代码阅读·异常流程　〔组员5 制作〕

题目：阅读代码，输出顺序是什么？

```python
try:
    x = int("8")
except ValueError:
    print("E")
else:
    print("OK")
finally:
    print("END")
```

- A. E → END
- B. OK → END
- C. 只输出 OK
- D. 只输出 END

答案：B

解析：int('8') 不会触发 ValueError，所以执行 else 输出 OK；finally 无论如何都会执行，继续输出 END。

### 151. 代码阅读·字典排序　〔组员5 制作〕

题目：阅读代码，print(result[0]) 的输出是什么？

```python
scores = {"Li": 88, "Wang": 95, "Zhao": 90}
result = sorted(scores.items(), key=lambda item: item[1], reverse=True)
print(result[0])
```

- A. ('Li', 88)
- B. ('Wang', 95)
- C. ('Zhao', 90)
- D. ('Wang', 88)

答案：B

解析：lambda item: item[1] 表示按分数排序，reverse=True 表示降序，最高分是 Wang 的 95。

### 152. 代码阅读·二维列表　〔组员5 制作〕

题目：阅读代码，print(list(zip(*matrix))) 的输出是什么？

```python
matrix = [[1, 2, 3], [4, 5, 6]]
print(list(zip(*matrix)))
```

- A. [(1, 4), (2, 5), (3, 6)]
- B. [[1, 4], [2, 5], [3, 6]]
- C. [(1, 2, 3), (4, 5, 6)]
- D. [1, 4, 2, 5, 3, 6]

答案：A

解析：*matrix 会把两行拆开传给 zip，zip 按列配对，所以得到三组元组：(1,4)、(2,5)、(3,6)。

### 153. 代码阅读·递归　〔组员5 制作〕

题目：阅读代码，程序输出什么？

```python
def digit_sum(n):
    if n == 0:
        return 0
    return n % 10 + digit_sum(n // 10)

print(digit_sum(305))
```

- A. 8
- B. 35
- C. 305
- D. 递归错误

答案：A

解析：digit_sum 每次取个位并递归处理剩余数字：305 的各位数字和是 3 + 0 + 5 = 8。

### 154. 随机模块

题目：random.sample(pop, k) 的作用是？

- A. 从序列中随机取 k 个不重复元素组成列表
- B. 返回一个 0~1 的随机小数
- C. 原地打乱序列
- D. 返回最大的 k 个元素

答案：A

解析：sample 从序列里随机抽 k 个不重复元素，不改变原序列，常用于抽奖。

### 155. 集合运算

题目：求两个集合的并集，用哪个运算符？

- A. |
- B. &
- C. -
- D. ^

答案：A

解析：并集 |（union）、交集 &（intersection）、差集 -、对称差 ^。

### 156. 集合运算

题目：集合运算中，& 表示？

- A. 交集
- B. 并集
- C. 差集
- D. 对称差

答案：A

解析：& 是交集（两个集合都有的元素），对应方法 intersection()。

### 157. del与clear

题目：清空列表全部元素但保留列表对象本身，用？

- A. lst.clear()
- B. del lst
- C. lst.remove()
- D. lst.pop(all)

答案：A

解析：clear() 清空所有元素、容器还在；del lst 把整个变量都删掉。

### 158. 可变参数

题目：函数形参 *args 会把多个位置参数收集成？

- A. 元组
- B. 列表
- C. 字典
- D. 字符串

答案：A

解析：* 前缀收集任意个位置参数，存为元组；** 收集关键字参数存为字典。

### 159. 可变参数

题目：函数形参 **kwargs 会把关键字参数收集成？

- A. 字典
- B. 元组
- C. 列表
- D. 集合

答案：A

解析：** 前缀把 key=value 形式的关键字参数收集成字典。

### 160. 模块导入

题目：from math import sqrt 的作用是？

- A. 只引入 math 模块里的 sqrt 函数
- B. 引入整个 math 模块
- C. 定义一个 sqrt 函数
- D. 删除 sqrt

答案：A

解析：from 模块 import 名字 只引入指定成员，可直接写 sqrt()；import math 则要写 math.sqrt()。

### 161. 函数返回

题目：函数里写 return a, b，实际返回的是？

- A. 一个元组 (a, b)
- B. 两个独立的值
- C. 一个列表 [a, b]
- D. 报错

答案：A

解析：返回多个值时 Python 会打包成一个元组返回（本质仍是一个返回值）。

### 162. 排序

题目：sorted(lst) 和 lst.sort() 的区别是？

- A. sorted 返回新列表不改原；sort 原地排序无返回
- B. 完全一样
- C. sorted 原地改；sort 返回新列表
- D. 两个都会报错

答案：A

解析：sorted() 返回排好序的新列表、原列表不变；list.sort() 直接在原列表上排序，返回 None。

### 163. 代码阅读·lambda

题目：阅读代码，print 输出什么？

```python
students = [{"name": "A", "age": 20, "grade": 85},
            {"name": "B", "age": 18, "grade": 92}]
best = max(students, key=lambda s: s["grade"])
print(best["name"])
```

- A. B
- B. A
- C. 92
- D. 85

答案：A

解析：max 用 key=lambda 取每人的 grade 作比较，最高分 92 是 B，所以输出 B 的名字。

### 164. 代码阅读·列表推导式

题目：阅读代码，print(even) 输出什么？

```python
nums = [1, 2, 3]
squared = [x ** 2 for x in nums]
even = [x for x in squared if x % 2 == 0]
print(even)
```

- A. [4]
- B. [1, 4, 9]
- C. [2]
- D. [4, 9]

答案：A

解析：squared = [1, 4, 9]；再筛选偶数只剩 4，所以 even = [4]。

### 165. 代码阅读·列表方法

题目：阅读代码，print(nums) 输出什么？

```python
nums = [1, 2, 3]
nums.append(4)
nums.insert(0, 0)
nums.remove(2)
print(nums)
```

- A. [0, 1, 3, 4]
- B. [0, 1, 2, 3, 4]
- C. [1, 3, 4]
- D. [0, 1, 2, 4]

答案：A

解析：append(4)→[1,2,3,4]；insert(0,0)→[0,1,2,3,4]；remove(2) 删第一个 2→[0,1,3,4]。

### 166. 代码填空·递归

题目：补全 Fibonacci 递归函数，____ 处应填什么？

```python
def fib(n):
    if n <= 0:
        return 0
    if n == 1:
        return 1
    return ____

print(fib(6))
```

- A. fib(n-1) + fib(n-2)
- B. fib(n-1) * fib(n-2)
- C. n-1 + n-2
- D. fib(n) + fib(n-1)

答案：A

解析：Fibonacci 第 n 项 = 前两项之和，递归调用 fib(n-1) + fib(n-2)。

### 167. type与id

题目：id() 函数返回的是？

- A. 对象的唯一标识（内存地址）
- B. 对象的类型
- C. 对象的长度
- D. 对象的值

答案：A

解析：id() 返回对象唯一标识（通常是内存地址），可判断两个变量是否指向同一对象；type() 才是看类型。

### 168. 文件模式

题目：文件模式 'r+' 与 'w+' 的区别是？

- A. r+ 不清空原内容，w+ 会先清空
- B. 完全一样
- C. r+ 只能读
- D. w+ 只能读

答案：A

解析：两者都能读写；但 r+ 保留原内容，w+ 打开就清空原文件。

### 169. 算法

题目：二分查找（折半查找）算法只能用于哪种数组？

- A. 有序数组
- B. 无序数组
- C. 嵌套数组
- D. 空数组

答案：A

解析：二分查找每次比较中间值来缩小范围，前提是数组已经有序。

### 170. 算法复杂度

题目：时间复杂度 O(log n) 通常对应下列哪个算法？

- A. 二分查找
- B. 顺序遍历
- C. 冒泡排序
- D. 暴力枚举

答案：A

解析：二分查找每次把范围减半，比较次数约 log₂n，复杂度 O(log n)。

### 171. 算法复杂度

题目：冒泡排序的时间复杂度是？

- A. O(n²)
- B. O(1)
- C. O(log n)
- D. O(n)

答案：A

解析：冒泡排序两层嵌套循环，平均和最坏时间复杂度都是 O(n²)。

### 172. 排序

题目：快速排序的核心思想是？

- A. 选基准划分 + 分治递归
- B. 相邻元素逐个交换
- C. 从头到尾顺序查找
- D. 递归累加求和

答案：A

解析：快排选一个基准，把更小/更大的元素分到两边（划分），再对两边递归，属于分治法。

### 173. 数论

题目：完数（完美数）的定义是？

- A. 所有真因数之和等于自身
- B. 全部因数之和等于自身
- C. 只能被 1 整除
- D. 各位立方和等于自身

答案：A

解析：完数等于它所有真因数（不含自身）之和，例如 6 = 1 + 2 + 3。

### 174. 字符串算法

题目：回文字符串指的是？

- A. 正读反读完全一致
- B. 全部由数字组成
- C. 长度一定是偶数
- D. 不含大写字母

答案：A

解析：回文正读反读相同，如 'level'、'aba'；可用 s == s[::-1] 判断。

### 175. 集合

题目：给列表去重，最快的数据结构转换是？

- A. 转成集合 set
- B. 转成元组
- C. 转成字符串
- D. 转成字典的值

答案：A

解析：set() 自动去重、查重接近 O(1)，list(set(lst)) 是最快的去重写法（不保序）。

### 176. 集合

题目：“哈希去重”在 Python 中通常对应哪种数据结构？

- A. set
- B. list
- C. tuple
- D. str

答案：A

解析：set/dict 底层是哈希表，成员判断快，适合去重和查重。

### 177. 算法

题目：下列哪一项属于“空间换时间”的优化思路？

- A. 用哈希表记录已遍历的值
- B. 二分查找
- C. 冒泡排序
- D. 顺序循环逐个比较

答案：A

解析：用额外的集合/字典记录已出现的值，用空间换取查找时间，是典型的空间换时间。

### 178. 应用

题目：常见的密码强度校验，一般“不”要求下列哪一项？

- A. 必须包含空格
- B. 长度不少于 8 位
- C. 包含大写字母
- D. 包含数字

答案：A

解析：强密码常要求长度、大小写、数字、符号，但一般不要求“必须含空格”。

### 179. 应用

题目：18 位身份证号的最后一位校验码，可能出现的特殊字符是？

- A. X
- B. Y
- C. Z
- D. Q

答案：A

解析：校验码若计算结果为 10，用罗马数字 X 表示，所以末位可能是 X。

### 180. 算法

题目：解决“三数之和”问题，常用的第一步操作是？

- A. 先对数组排序
- B. 先反转数组
- C. 先转成集合
- D. 先统计长度

答案：A

解析：先排序便于用双指针向中间逼近并跳过重复值，是三数之和的常用第一步。

### 181. 代码阅读·变量赋值

题目：阅读代码，print(b) 输出什么？

```python
a = 12
b = a
a = 50
print(b)
```

- A. 12
- B. 50
- C. 0
- D. 报错

答案：A

解析：b = a 时把 12 赋给 b；之后 a 改成 50 不影响已赋值的 b，输出 12。

### 182. 代码阅读·切片

题目：阅读代码，print(s[:4]) 输出什么？

```python
s = "programming"
print(s[:4])
```

- A. prog
- B. progr
- C. gram
- D. pro

答案：A

解析：s[:4] 取前 4 个字符（索引 0~3），得到 'prog'。

### 183. 代码阅读·参数传递

题目：阅读代码，print(num) 输出什么？

```python
def add(x):
    x += 10

num = 20
add(num)
print(num)
```

- A. 20
- B. 30
- C. 10
- D. 报错

答案：A

解析：整数不可变，函数内 x += 10 只改局部副本，原 num 仍是 20。

### 184. 代码阅读·运算符

题目：阅读代码，print(res) 输出什么？

```python
res = 14 // 3 + 2 ** 3
print(res)
```

- A. 12
- B. 10
- C. 13
- D. 9

答案：A

解析：先幂 2**3=8，再整除 14//3=4，最后 4+8=12（** 高于 //，// 高于 +）。

### 185. 代码阅读·循环

题目：阅读代码，输出是什么？

```python
for i in range(4, 15, 5):
    print(i, end=" ")
```

- A. 4 9 14
- B. 4 5 6
- C. 5 10 15
- D. 4 8 12

答案：A

解析：从 4 起、步长 5：4、9、14（下一个 19≥15 停），end=' ' 同行空格分隔。

### 186. 代码阅读·递归

题目：阅读代码，print(sum_all(5)) 输出什么？

```python
def sum_all(n):
    if n == 1:
        return 1
    return n + sum_all(n - 1)

print(sum_all(5))
```

- A. 15
- B. 5
- C. 10
- D. 8

答案：A

解析：1+2+3+4+5=15，递归到 n==1 返回 1，再逐层累加。

### 187. 代码阅读·列表推导式

题目：阅读代码，print(new) 输出什么？

```python
lst = [4, 6, 8, 10]
new = [x // 2 for x in lst]
print(new)
```

- A. [2, 3, 4, 5]
- B. [8, 12, 16, 20]
- C. [0, 0, 0, 0]
- D. [4, 6, 8, 10]

答案：A

解析：各元素整除 2：4→2, 6→3, 8→4, 10→5。

### 188. 代码阅读·转义字符

题目：阅读代码，输出是什么？

```python
print("apple\nbanana\npear")
```

- A. 分三行输出三个单词
- B. 一行输出全部文字
- C. 原样输出 apple\nbanana\npear
- D. 运行报错

答案：A

解析：\n 是换行符，apple、banana、pear 分三行输出。

### 189. 代码阅读·条件

题目：阅读代码，输出是什么？

```python
x = 3
if x > 5:
    print("大")
else:
    print("小")
```

- A. 小
- B. 大
- C. 无输出
- D. 报错

答案：A

解析：3 > 5 不成立，走 else 输出“小”。

### 190. 代码阅读·循环

题目：阅读代码，print(count) 输出什么？

```python
count = 0
for i in range(1, 4):
    count += 1
print(count)
```

- A. 3
- B. 1
- C. 4
- D. 0

答案：A

解析：range(1, 4) 循环 3 次，count 从 0 累加到 3。

### 191. 代码阅读·字典

题目：阅读代码，print(d.get('z', 0)) 输出什么？

```python
d = {"x": 10, "y": 20}
print(d.get("z", 0))
```

- A. 0
- B. 10
- C. 20
- D. 报错

答案：A

解析：键 'z' 不存在，get 返回默认值 0，不会报错。

### 192. 代码阅读·交换

题目：阅读代码，print(a, b) 输出什么？

```python
a, b = 7, 9
a, b = b, a
print(a, b)
```

- A. 9 7
- B. 7 9
- C. 7 7
- D. 9 9

答案：A

解析：a, b = b, a 同时交换，a 变 9、b 变 7。

### 193. 代码阅读·索引

题目：阅读代码，print(word[-2]) 输出什么？

```python
word = "test"
print(word[-2])
```

- A. s
- B. t
- C. e
- D. 报错

答案：A

解析：-2 是倒数第 2 个字符，'test' 倒数第二个是 's'。

### 194. 代码阅读·循环累加

题目：阅读代码，print(total) 输出什么？

```python
total = 0
for i in [1, 2, 3]:
    total = total + i
print(total)
```

- A. 6
- B. 3
- C. 5
- D. 0

答案：A

解析：依次累加 1+2+3 = 6。

### 195. 代码阅读·布尔

题目：阅读代码，输出是什么？

```python
flag = False
if not flag:
    print("yes")
else:
    print("no")
```

- A. yes
- B. no
- C. 无输出
- D. 报错

答案：A

解析：not False 为 True，进入 if 输出 'yes'。
